<div class="flex items-center justify-between gap-4 h-6">
  <div></div>
  <div></div>
</div>

<header class="mt-8 mb-12">
  <div class="flex items-baseline justify-between gap-6">
    <h1 class="ui-title truncate min-w-0">Remember</h1>
    <div class="shrink-0"></div>
  </div>
</header>

<ol class="space-y-20">
  <li class="hidden last:block text-white/60 italic">Nothing due. Come back later.</li>
  <% @due_cards.each do |card| %><li id="<%= dom_id(card) %>"><%= render card %></li><% end %>
</ol>
